---

num_rounds: 2 # number of FL rounds in the experiment
num_clients: 2 # number of total clients in the experiment
batch_size: 128 # batch size to use by the clients
input_size: 6 # input size of the model
hidden_size: 16 # hidden size of the model
output_size: 2 # output size of the model